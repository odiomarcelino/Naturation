# Naturation

A minimal Next.js site showing an animated natural scene. The animation plays automatically on page load and fills the screen, creating a calming atmosphere. Deploy it to Vercel for a lightweight digital art piece.

## Development

```bash
npm install
npm run dev
```

Open `http://localhost:3000` in your browser.
